#
# config.xml — LLVM 主机工具链源码配置
#
# 本文件被 prepare.sh 和 build.sh 以 `source` 方式读取。
#
# LLVM        仓库名（git clone 时使用）
# LLVM_BRANCH 分支名（同时用于推导 LLVM 版本号：dev_17.0.6 → 17.0.6）
#

LLVM="llvm-project"
LLVM_BRANCH="dev_17.0.6"
