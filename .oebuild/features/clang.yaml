type: feature

support: qemu-aarch64|raspberrypi4-64

local_conf: |
  DISTRO_FEATURES_append = " clang "
  DISTRO_FEATURES_NATIVE_append = " clang "
  EXTERNAL_TOOLCHAIN_CLANG_BIN = "${EXTERNAL_TOOLCHAIN_aarch64}/bin"

layers:
- yocto-meta-openeuler/meta-clang
