id: k3s
name: K3s Kubernetes
prompt: Enable K3s (default k3s-agent)
dependencies: [containers]
machines: [qemu-aarch64, phytium]
one_of:
  - self/k3s-agent
  - self/k3s-server
default_one_of: self/k3s-agent
config:
  repos:
    - yocto-meta-virtualization
  layers:
    - yocto-meta-virtualization

sub_feats:
  - id: k3s-agent
    name: K3s Agent
    config:
      local_conf:
        - 'DISTRO_FEATURES:append = " k3s-agent "'
        - 'IMAGE_INSTALL:append = " packagegroup-k3s-agent "'

  - id: k3s-server
    name: K3s Server
    config:
      local_conf:
        - 'DISTRO_FEATURES:append = " k3s-server "'
        - 'IMAGE_INSTALL:append = " packagegroup-k3s-server "'
