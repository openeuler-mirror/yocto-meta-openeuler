id: debug
name: Debug tooling
prompt: Include debug packages and helpers
config:
  local_conf:
    - 'IMAGE_FEATURES:append = " tools-debug dbg-pkgs debug-tweaks"'
    - 'DISTRO_FEATURES:append = " haveged"'
