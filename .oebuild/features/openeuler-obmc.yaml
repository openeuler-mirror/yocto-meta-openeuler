type: feature

support: qemu-aarch64

layers:
- yocto-meta-phosphor

local_conf: |
  DISTRO_FEATURES:append = " obmc "
  INIT_MANAGER = "systemd"
  VIRTUAL-RUNTIME_dev_manager = "systemd" 

repos:
  yocto-meta-phosphor:
    url: https://gitee.com/yongmao_luo/yocto-meta-phosphor.git
    path: yocto-meta-phosphor
    refspec: kirkstone
