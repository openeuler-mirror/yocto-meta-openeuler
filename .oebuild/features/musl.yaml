type: feature

support: aarch64-std|raspberrypi4-64

local_conf: |
  EXTERNAL_TARGET_SYS_aarch64 = "aarch64-openeuler-linux-musl"
  LIBC = "musl"
  crypt = "musl"
  TCMODE-LIBC = "musl"
  MACHINE_ESSENTIAL_EXTRA_RDEPENDS = "musl"

layers:
- yocto-meta-openeuler/meta-musl
