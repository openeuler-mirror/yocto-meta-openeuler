id: oebridge-kernel-dev
name: oebridge for kernel development
prompt: openEuler server repo plus kernel development integration
dependencies: [oebridge]
config:
  local_conf:
    - 'DISTRO_FEATURES:append = " kdev "'
