id: oebridge-ibrobot
name: oebridge for IB Robot (ROS)
prompt: openEuler server repo plus IB Robot (ROS) integration
dependencies: [oebridge]
config:
  local_conf:
    - 'INIT_MANAGER = "systemd"'
    - 'DISTRO_FEATURES:append = " ibrobot oe-ros"'
