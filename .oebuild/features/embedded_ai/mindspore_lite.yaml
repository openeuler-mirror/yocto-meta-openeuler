id: mindspore_lite
name: MindSpore Lite
prompt: Enable MindSpore Lite AI inference + demo-classification
machines: [qemu-aarch64]
config:
  local_conf:
    - 'DISTRO_FEATURES:append = " mindspore-lite "'
