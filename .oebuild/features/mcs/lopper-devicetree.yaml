id: lopper-devicetree
name: Peripheral Partition (Lopper)
prompt: Enable peripheral partition management via lopper device tree operations
machines: [qemu-aarch64, raspberrypi4-64]
config:
  local_conf:
    - 'MCS_FEATURES:append = " lopper-devicetree "'
