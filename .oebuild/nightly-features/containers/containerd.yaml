id: containerd
name: Containerd runtime
prompt: Use containerd as the runtime
support: true
dependencies: [containers]
machines: [qemu-aarch64, phytiumpi]
config:
  layers:
    - yocto-meta-virtualization
  local_conf:
    - 'DISTRO_FEATURES:append = " containerd "'
