id: kubeedge
name: KubeEdge edge stack
prompt: Enable KubeEdge
dependencies: [containers, isulad]
machines: [qemu-aarch64, raspberrypi4-64, hieulerpi1]
config:
  local_conf:
    - 'DISTRO_FEATURES:append = " kubeedge isulad "'
