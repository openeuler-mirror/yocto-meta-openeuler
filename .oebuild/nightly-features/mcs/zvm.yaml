id: zvm
name: ZVM support
prompt: Enable ZVM virtualization
dependencies: [mcs]
machines: [qemu-aarch64]
config:
  local_conf:
    - 'MCS_FEATURES:append = " zvm "'
    - 'DISTRO_FEATURES_NATIVE:append = " mcs "'
