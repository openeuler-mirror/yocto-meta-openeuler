id: micrun
name: shimv2 runtime
prompt: Enable the Micrun shimv2 integration, compatible with containerd 1.7.19+, work for xen as hypervisor
machines: [qemu-aarch64, phytiumpi]
dependencies: [mcs, mcs/rtos, mcs/xen, containers]
config:
  local_conf:
    - 'MCS_FEATURES:append = " micrun "'
