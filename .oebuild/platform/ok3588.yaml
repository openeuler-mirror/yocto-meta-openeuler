type: platform

machine: ok3588

toolchain_type: EXTERNAL_TOOLCHAIN:aarch64

repos:
- yocto-meta-rockchip

layers:
  - yocto-meta-rockchip

local_conf: |
  PREFERRED_PROVIDER_virtual/kernel ?= "linux-openeuler"
